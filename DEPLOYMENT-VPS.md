> 별도 VPS를 선택할 때만 사용하는 대안입니다. 기본 배포 대상은 Cloudflare이며 DEPLOYMENT.md를 먼저 보세요.

# 환승로그 공개 배포 준비

현재 버전은 로컬에서 화면과 Node API를 함께 제공합니다. Docker Compose로 동일한 구성을 서버 한 대에 올릴 수 있도록 준비했습니다. 실제 서버 생성, 도메인 연결, 외부 HTTPS 접속 검증은 아직 진행하지 않았습니다. 기존 정적 공개 사이트에는 이 변경이 자동 반영되지 않습니다.

## 권장 구성과 예상 비용

- AWS Lightsail 서울 리전의 Linux 인스턴스, public IPv4 포함, 2 GB 메모리: 기본 월 **US$12**. 세금·도메인·선택한 부가 서비스와 초과 사용료는 별도입니다. 가입 화면에서 리전별 제공 여부와 최종 견적을 다시 확인합니다.
- 인스턴스에 Lightsail 고정 IPv4를 연결합니다. ODsay Server 앱에 이 주소를 등록해야 합니다. 기존 로컬 PC의 허용 IP는 서버 IP와 다릅니다.
- 직접 관리할 수 있는 도메인의 A 레코드를 고정 IP로 연결합니다. 기존 `chatgpt.site` 주소는 DNS 관리 권한을 확인하지 않았으므로 이 서버용으로 재사용한다고 가정하지 않습니다.
- Caddy가 80/443 포트를 받고 HTTPS 인증서를 관리합니다. Node 4173 포트는 외부에 공개하지 않습니다.

가격 근거: [Lightsail 인스턴스 번들](https://docs.aws.amazon.com/lightsail/latest/userguide/amazon-lightsail-bundles.html), [공식 가격표](https://aws.amazon.com/lightsail/pricing/). 구성 근거: [고정 IP 연결](https://docs.aws.amazon.com/lightsail/latest/userguide/lightsail-create-static-ip.html), [Caddy 자동 HTTPS](https://caddyserver.com/docs/automatic-https).

## 서버에 올리는 순서

1. 서버 구매 승인 후 인스턴스와 고정 IP를 준비합니다. 과금이 발생하는 인스턴스 생성은 승인 전에 실행하지 않습니다.
2. 서버 방화벽에서 웹 80/TCP, 443/TCP를 허용하고 SSH 22/TCP는 관리자 IP로 제한합니다. 443/UDP는 HTTP/3용 선택 항목입니다. 4173은 열지 않습니다. IPv6를 사용한다면 별도 방화벽 규칙도 확인합니다.
3. [Docker 공식 Ubuntu 설치 안내](https://docs.docker.com/engine/install/ubuntu/)에 따라 Docker Engine과 Compose 플러그인을 설치합니다.
4. `hwanseung-log-deploy.zip`을 서버의 전용 앱 폴더에 풉니다. 이 파일에는 인증키가 없습니다.
5. `.env.example`을 `.env`로 복사하고 기존 승인된 네 API 키를 서버의 `.env`에만 입력합니다. `DOMAIN`은 `환승로그 도메인`의 실제 영문 호스트명으로 바꿉니다. `https://`나 경로는 넣지 않습니다. `.env` 접근 권한은 소유자 읽기·쓰기만 허용합니다.
6. ODsay 콘솔에서 환승로그 Basic 앱의 허용 Server IP에 고정 IP를 반영합니다. 다른 제공기관에 IP 제한을 설정했다면 해당 설정도 확인합니다. 승인된 API 사용 한도를 먼저 확인하고 서버 상한을 그 이하로 설정합니다.
7. DNS가 연결된 뒤 앱 폴더에서 실행합니다.

```sh
chmod 600 .env
docker compose config --quiet
docker compose up -d --build
docker compose ps
```

`docker compose config`는 `--quiet` 없이 실행하면 해석된 환경변수에 키가 노출될 수 있으므로 출력물을 공유하지 않습니다. 앱 이미지는 `.env`를 복사하지 않고 실행 시 주입받습니다.

## 배포 후 확인

- `https://도메인/healthz`: `{ "ok": true }` 응답. 프로세스 건강 상태이며 외부 API 연결 성공을 의미하지는 않습니다.
- `/api/status`: 네 제공기관 설정 여부를 확인합니다. 키 값은 응답에 없습니다.
- 관광정보 불러오기, 오늘 운항하는 정확한 편명 조회, 공항 왕복 길찾기를 각각 실제 조회합니다.
- 당일 코스를 골라 **지금 정보로 일정 업데이트 → 조회하고 비교하기 → 이 일정으로 반영하기**를 실행합니다. 다른 기기의 모바일 브라우저에서도 확인합니다.
- 재시작 후에도 호출 횟수가 유지되는지 확인하고, HTTPS·정적 자산·API 오류 응답을 확인합니다.

## 호출량과 운영 범위

기본 서버 상한은 ODsay 30회/일, Kakao 500회/일, 인천공항 500회/일, TourAPI 500회/일입니다. 이 수치는 제공기관의 잔여량 조회 결과가 아니라 앱에서 설정한 보호 한도입니다. 한국시간 자정에 날짜가 바뀝니다. 실패한 시도도 보수적으로 집계하며, 다른 도구에서 같은 키로 사용한 호출은 이 서버 장부에 포함되지 않습니다.

한 장소 왕복 일정 업데이트는 교통 API 2회, 두 장소는 보통 3회입니다. 따라서 ODsay Basic 30회는 하루 약 10~15번의 코스 갱신만으로도 소진될 수 있습니다. 별도 길찾기도 같은 한도를 사용합니다. 본격 공개 서비스 전에 ODsay 요금제·이용 허가·한도 확대를 결정해야 합니다. 로그인 없는 시연 서비스이므로 여러 방문자가 전체 한도를 공유합니다. 이 장치가 악의적인 한도 소진을 완전히 방지하지는 않습니다.

방문자별 분당 최대 12회, 서버 전체 동시 API 요청 4개를 허용합니다. `/api/places` 한 번은 상위 TourAPI 요청 8회로 집계합니다. 초과하면 JSON 오류와 `Retry-After`를 반환합니다. 응답 데이터는 서버에 캐시하지 않습니다. `usage` 볼륨에는 날짜와 제공기관별 호출 횟수만 저장합니다. 장부 손상·쓰기 실패 때에는 API 호출을 중단합니다.

이 장부는 **Node 프로세스 한 개, 앱 컨테이너 한 개**에 맞춰져 있습니다. 여러 서버·프로세스로 확장하려면 Redis 등 공유 원자적 카운터로 교체해야 합니다. `docker compose down -v`는 호출 장부와 인증서 볼륨을 삭제하므로 업데이트에 사용하지 않습니다. `TRUST_PROXY=true`는 4173 포트를 내부에만 둔 제공된 Caddy 구성에서 사용합니다. CDN을 앞에 두면 실제 방문자 IP 전달과 신뢰 범위를 따로 검토해야 합니다.

## 업데이트와 되돌리기

이전 소스 배포본을 보관하고 `.env` 및 `usage`, `caddy_data`, `caddy_config` 볼륨은 유지합니다. 새 코드를 올린 뒤 `docker compose up -d --build`로 적용합니다. 문제가 있으면 이전 소스를 복원한 뒤 같은 명령으로 재시작합니다. API 키와 이용 한도는 되돌리지 않습니다. 같은 날짜에 로컬 실행에서 서버로 전환할 경우 기존 사용량까지 고려해 당일 상한을 낮춥니다.

## 일정에 반영되는 정보

오늘의 당일 코스에서만 갱신합니다. 선택한 코스의 모든 이동 구간과 요금을 확인한 후 현재 시각, 도착 지연, 더 이른 출발 시각을 계산에 반영합니다. 출발 지연은 복귀 마감을 늦추지 않습니다. 터미널 불일치, 날짜가 모호한 운항 시각, 취소·회항·이미 출발한 편명은 적용을 막습니다. 실패·예산 초과·휴관·복귀 마감 초과 때에는 기존 코스를 유지하고 이유를 안내합니다.

조회 응답은 화면에서 비교하고 사용자가 적용합니다. 10분이 지난 미리보기나 일정이 바뀐 미리보기는 적용하지 않습니다. 저장되는 것은 계산된 개인 일정과 확인 시각이며 제공기관의 원본 응답은 저장하지 않습니다. 이후 다른 코스로 바꾸면 교통 정보를 다시 확인해야 합니다. 현재 조회한 이동시간은 미래 출발 시각별 배차·막차·교통 예측이 아닙니다. 운영시간·입장료·입국 자격·체크인 마감은 별도 확인이 필요합니다.

## 현재 검증 범위

로컬 자동 테스트, 정적 빌드, Compose 구성 검사 및 브라우저 확인을 수행합니다. 이 PC의 Docker 엔진이 실행 중이 아니므로 컨테이너 실행·Caddy 인증서 발급·외부 고정 IP에서의 ODsay 호출은 실제 서버에서 완료해야 합니다.
