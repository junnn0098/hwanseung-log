# 환승로그 · Cloudflare 배포

기본 배포 대상은 **Cloudflare Workers + Static Assets**입니다. 화면과 `/api/*`를 한 프로젝트에서 제공합니다. 별도 AWS 서버, Docker, Caddy, 고정 IP 서버를 신청하지 않아도 되는 구성입니다. 기존 로컬 Node 실행은 계속 지원합니다.

공개 주소: **https://hwanseung-log.jum4769.workers.dev** · 2026-09-21 배포 완료. 관광·항공·Kakao 서버 인증키와 ODsay Web 키는 Cloudflare Secrets에 저장했습니다. 공개 사이트에서 대중교통 경로까지 조회했습니다.

## 구성

| 기능 | 실행 위치 | 필요한 설정 |
|---|---|---|
| 웹 화면·이미지·일정 계산 | Workers Static Assets + 브라우저 | `public/` |
| TourAPI·인천공항·Kakao | Cloudflare Worker | 세 가지 서버용 인증키를 Secrets로 저장 |
| ODsay 대중교통 | 이용자 브라우저 → ODsay | 별도의 **Web 플랫폼 키**, 배포 도메인 등록 |
| 공통 일일 호출 상한 | SQLite 기반 Durable Object | `API_GATE` 바인딩과 마이그레이션 자동 배포 |
| 개인 일정·메모 | 브라우저 로컬 저장소 | 기존과 동일 |

Cloudflare가 제공하는 `workers.dev` 주소로 시작할 수 있습니다. 직접 구매한 도메인은 선택사항입니다. [Static Assets 공식 안내](https://developers.cloudflare.com/workers/static-assets/)와 [Durable Objects 무료 플랜 지원](https://developers.cloudflare.com/durable-objects/platform/pricing/)에 맞춘 구성입니다. 무료 한도 내 사용으로 시작하며, 무제한 무료를 보장하지 않습니다. 유료 플랜으로 자동 전환하거나 결제 설정을 변경하는 절차는 포함하지 않습니다.

## 중요한 ODsay 구분

현재 로컬에 있는 `ODSAY_API_KEY`는 **Server 플랫폼 키**입니다. 호출 서버의 IP를 확인하는 인증 방식이므로 Cloudflare에 그대로 복사해서 정상 동작한다고 가정하면 안 됩니다. 도메인을 Cloudflare DNS에 연결하는 것만으로 Worker의 외부 요청 IP가 고정되는 것도 아닙니다.

Cloudflare 배포에서는 ODsay의 **Web 플랫폼** 키를 별도로 발급하고, 실제 배포 주소를 허용 도메인으로 등록합니다. [ODsay 공식 답변](https://lab.odsay.com/community/boardView?seq=716)은 Web 키를 등록한 도메인의 프런트엔드에서 사용하도록 안내합니다. Worker에서 Referer를 위조해 Web 키를 대신 호출하는 방식은 사용하지 않습니다.

- `ODSAY_WEB_PUBLIC_KEY`는 브라우저에 전달되는 공개용 키입니다. 기존 Server 키를 이 변수에 절대 넣지 않습니다.
- Web 키는 허용 도메인 제한이 필요하며, 이 앱의 일일 예약 한도 외에도 제공기관의 실제 호출 한도에 영향을 받습니다.
- Web 키가 없으면 대중교통은 연결 준비 중이라고 명시합니다. 가짜 실시간 결과를 보여주거나 기존 일정에 덮어쓰지 않습니다.
- 로컬 `npm start`는 기존 Server 키를 계속 사용합니다. Workers 로컬 검증은 Web 플랫폼에 등록한 개발 주소와 별도 Web 키가 필요합니다.

## 로컬 검증

Node.js 22 이상이 필요합니다. Wrangler는 고정된 개발 의존성이며 `npm ci`로 동일 버전을 설치합니다.

```sh
npm ci
npm test
npm run cf:check
```

Worker를 로컬에서 실행하려면 `.dev.vars.example`을 `.dev.vars`로 복사하고 해당 환경의 키를 입력합니다. `.dev.vars`와 `.env`는 배포 압축파일 및 Git에서 제외됩니다.

```sh
npm run cf:dev
```

주소는 `http://127.0.0.1:8787`입니다. 이 로컬 에뮬레이터는 공개 배포 성공을 의미하지 않습니다. 기존 로컬 개발은 `npm start`와 4173 포트로 계속할 수 있습니다.

## 실제 배포 순서

1. Cloudflare 계정으로 Wrangler를 인증합니다. 이미 같은 이름의 Worker가 있다면 내용을 확인하고 기존 서비스를 덮어쓰지 않도록 배포 이름을 먼저 결정합니다.
2. 프로젝트 루트에서 배포합니다. 이름은 `wrangler.jsonc`의 `hwanseung-log`입니다. 첫 배포 때 SQLite Durable Object도 생성합니다. 초기에는 키가 없어 API가 연결 준비 상태입니다.

```sh
npx wrangler login
npm run cf:deploy
```

3. 출력된 **실제 배포 URL**을 확인합니다. 계정별 `workers.dev` 하위 도메인은 로그인·배포 전에는 추측하지 않습니다.
4. Cloudflare 대시보드의 해당 Worker 설정에서 아래 변수를 **Secret** 형식으로 입력합니다. 또는 `npx wrangler secret put 변수명`으로 값 입력 프롬프트에 붙여 넣습니다. 키 값을 명령 인자나 소스코드에 넣지 않습니다.

| Secret 이름 | 값 |
|---|---|
| `TOUR_API_KEY` | 승인된 관광공사 인증키 |
| `AIRPORT_API_KEY` | 승인된 인천공항 인증키 |
| `KAKAO_REST_API_KEY` | 승인된 Kakao REST 키 |
| `ODSAY_WEB_PUBLIC_KEY` | 새로 발급한 ODsay Web 플랫폼 공개용 키 |

마지막 키는 설정 관리 편의를 위해 Secret에 보관하더라도 브라우저 호출 시 의도적으로 전달됩니다. 나머지 세 키 및 기존 ODsay Server 키는 브라우저로 보내지 않습니다.

5. ODsay Web 플랫폼 허용 도메인에 배포 URL의 호스트를 등록합니다. 현재 앱 `7261`의 URI에는 `hwanseung-log.jum4769.workers.dev`만 등록했습니다. 콘솔은 `https://` 같은 프로토콜을 제외한 도메인명을 요구합니다. 기존 Server IP와 키는 유지하며, 미리보기 주소나 와일드카드를 허용하지 않습니다.
6. 공개 사이트에서 관광정보, 오늘의 항공편, 택시, **대중교통의 브라우저 직접 호출과 도메인 인증**을 각각 실제 검증합니다. 다른 기기에서도 일정 갱신까지 확인합니다.

Cloudflare 설정 방식은 [Secrets 공식 문서](https://developers.cloudflare.com/workers/configuration/secrets/)를 따릅니다. 기존 `.openai/hosting.json`과 정적 공개 사이트는 별개이며, 자동으로 업데이트되지 않습니다. Pages에 `dist`만 업로드하면 API가 배포되지 않습니다. 이 앱의 기본 경로는 프로젝트 전체를 Workers로 배포하는 것입니다.

## 호출량 보호와 범위

- TourAPI 500회, 인천공항 500회, Kakao 500회, ODsay 30회의 일일 상한이 기본값입니다. 실제 계약·승인 한도 이하로 설정합니다. 한국시간 자정 기준입니다.
- 글로벌 한도가 Worker 실행 위치마다 초기화되지 않도록 이름이 고정된 Durable Object 하나를 사용합니다. 저장하는 것은 날짜와 제공기관별 합계뿐입니다.
- 방문자별 분당 12회, 동시 처리 4개 제한은 Durable Object 메모리 기준입니다. 재생성 시 짧은 구간 제한은 초기화될 수 있으나 일일 합계는 유지됩니다.
- TourAPI 장소 모음 요청 하나는 8회, 장소 상세의 공통·소개 정보 요청은 합계 2회로 계산합니다. 코스 갱신은 교통 2~3회가 필요합니다. ODsay Web 조회는 브라우저 요청을 허용하기 전에 1회 예약하며 실패해도 환불하지 않습니다.
- 공개 Web 키의 직접 사용까지 이 앱의 서버 장부가 강제로 제한할 수는 없습니다. 최종 사용량 제한은 ODsay의 도메인 인증 및 해당 플랜 한도에 의존합니다. 본격 공개 운영 전 사용량 확대가 필요할 수 있습니다.
- 인증키와 API 원본 응답, 방문자의 일정은 Durable Object에 저장하지 않습니다. 외부 API 요청 실패 시 원문 오류를 숨깁니다.
- Worker 변경 시 `API_GATE` 객체 이름과 마이그레이션을 임의로 바꾸면 장부가 새로 생길 수 있습니다. 같은 날 배포나 플랜 전환 시 제공기관 콘솔의 기존 사용량도 고려합니다.

## 변경과 되돌리기

이후 웹 수정은 같은 폴더에서 진행하고 `npm test`, `npm run cf:check` 후 `npm run cf:deploy`를 실행합니다. Cloudflare Worker 버전 기록에서 되돌릴 수 있습니다. Secrets와 Durable Object 저장소는 별도로 유지되므로 버전 되돌리기가 호출 장부를 초기화하는 방법이라고 가정하지 않습니다.

VPS용 구성은 선택 대안으로 보관되어 있으며 [DEPLOYMENT-VPS.md](DEPLOYMENT-VPS.md)에 설명합니다. Cloudflare 배포에는 사용하지 않습니다.


## 이번 검증 결과

2026-09-21 기준 자동 테스트 44개와 Wrangler 배포 사전 빌드를 통과했습니다. 공개 Cloudflare 주소에서 HTTPS 화면, 상태 응답, 관광공사 검색 응답(초기 16건; 1.1.1부터 이름·유형·위치를 대조한 8개 장소로 정제), 항공 API 정상 응답, SQLite Durable Object를 거친 실제 Kakao 경로 응답을 확인했습니다. 공개 브라우저에서도 송도 → 인천공항 T1 택시 조회 결과가 표시됐고 콘솔 오류는 없었습니다. 항공 조회에 사용한 KE123 출발편은 당일 결과가 0건이므로 특정 항공편 정보 표시까지 검증한 것은 아닙니다.

ODsay Web 키는 Server 키와 다른 값임을 확인하고 `ODSAY_WEB_PUBLIC_KEY` Secret으로 저장했습니다. 공개 브라우저에서 송도 → 인천공항 T1 경로가 55~58분, 303-1·303번 버스 2,300원으로 실제 조회됐습니다(조회 당시 값). 일부 공항버스의 요금 미제공 값 `-1`은 `null`로 정규화하고 화면에는 ‘요금 확인 필요’로 안내합니다. 자동 일정 갱신에서는 요금을 확인한 경로만 예산 계산에 사용합니다. ODsay Basic 앱의 현재 한도는 하루 30회이므로 방문자가 늘면 제공기관의 이용량 확대가 필요할 수 있습니다.

Web 키는 Cloudflare에만 저장했으며 로컬 `.dev.vars`에는 복사하지 않았습니다. 현재 등록 도메인은 공개 주소 한 곳이므로 로컬 Workers에서 Web 호출을 시험하려면 별도로 개발 주소를 등록해야 합니다. 기존 Node 로컬 실행은 계속 Server 키를 사용합니다.

이 PC에서 Wrangler 요청이 시간 초과될 때는 PowerShell에서 `$env:NODE_OPTIONS='--dns-result-order=ipv4first --no-network-family-autoselection'`을 설정한 뒤 다시 실행합니다. 이 설정으로 1.0.5 배포의 연결 지연이 해결됐습니다. 배포 로그나 인증 파일은 공개 압축파일에 포함하지 않습니다.

## 1.1.1 추가 검증

장소 상세에서 관광공사 `detailCommon2`와 `detailIntro2`를 호출합니다. 국립세계문자박물관의 운영시간, 월요일 휴관 및 공휴일 예외, 무료·기획전 별도 요금 문구를 실제 응답과 화면으로 확인했습니다. 장소별 정보 없음 및 부분 실패를 별도로 안내합니다. 단위 검증은 동명 장소 오연결 방지, 상세 조회 2회 차감, 당일·익일 시간 경계, 교통정보 경과 상태를 포함합니다.

## 최종 반영 기록

2026-09-21 12:30 KST 기준 1.1.1을 기존 공개 주소에 반영했습니다. Cloudflare 버전 ID: `b5ec7f77-f969-4801-9471-29faf0996eaa`. 공개 웹의 신규 장소 상세와 장소 제외 후 대체 코스 비교를 확인했습니다. 공개·로컬 상태 응답 모두 관광·항공·택시·대중교통 연결 설정이 확인됐고, 로컬 서버도 새 상세 API로 재시작했습니다. 로컬 관광 상세 조회는 한 차례 502 응답 후 재조회에서 정상 응답했습니다. 외부 API 일시 실패 가능성을 운영 시 고려해야 합니다.

## 1.2.1 공모전 데모 검증

공모전 데모, `/#demo` 직접 진입, 네 가지 시나리오 카드, 개인 여행과 데모의 분리 저장을 반영했습니다. 기존 개인 일정을 유지하며 새 개인 여행은 입국 미확인 상태로 시작합니다. 기본 시나리오는 외부 API 호출 없이 재현됩니다. 관광정보와 교통의 실제 조회는 별도 버튼을 통해 실행합니다.

자동 검증 48개, 배포 사전 빌드, 한국어·영어 모바일 화면, 시나리오 비교·적용·취소, 개인 일정 복원 및 새로고침 유지를 확인했습니다. 공개 배포에서도 기본 코스와 90분 지연 비교·적용을 확인했고 브라우저 콘솔 오류는 없었습니다. Cloudflare 버전 ID: `47337fef-22c0-466c-9268-ef77cddf8b2d`.

## 1.2.2 기본 홈 화면 복원

2026-09-21 기본 주소와 `/#home` 진입을 여행 소개 화면·개인 여행 모드로 복원했습니다. 이전에 데모를 이용했더라도 기본 주소는 홈 화면으로 열립니다. 데모는 선택 버튼과 `/#demo` 링크로 이용하며, 내부 일정 화면 새로고침과 개인·데모 저장 분리는 유지합니다.

자동 검증 48개와 배포 사전 빌드를 통과했습니다. 실제 공개 주소에서 여행 소개 화면과 개인 여행 모드 표시를 확인했습니다. Cloudflare 버전 ID: `9b41c1e4-e9fe-4608-9a38-bdfd217d9bf0`.
