{
  "name": "hwanseung-log",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "start": "node --env-file-if-exists=.env server.mjs", "dev": "node --env-file-if-exists=.env server.mjs", "test": "node --test tests/*.test.mjs", "build": "node scripts/build.mjs" },
  "engines": { "node": ">=22" }
}
