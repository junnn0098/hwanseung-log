import {state,t} from './state.js?v=1.2.2';
import {icon,button} from './components.js?v=1.2.2';

export function modeBar(){
 if(state.mode!=='demo')return `<div class="mode-bar container personal-mode"><span>${t('나의 여행 모드','Your personal trip')}</span>${button(t('공모전 데모 보기','Explore the demo'),'enter-demo','ghost small')}</div>`;
 return `<div class="mode-bar container"><div><span class="demo-pill">DEMO</span><span>${t('공모전 체험 모드','Competition demo')}<small>${t('예시 일정 · 개인 여행과 별도 저장','Sample itinerary · stored separately from your trip')}</small></span></div>${button(t('내 일정으로 시작하기','Use my own trip')+icon('arrow-up-right'),'personal-mode','small')}</div>`;
}
export function demoStage(){
 const scenes=[['base','route','기본 여행','The original plan','송도 산책과 실내 문화 여행','A park stroll and an indoor discovery'],['rain','cloud-rain','갑자기 비가 오면','What if it rains?','야외 장소를 뺀 코스 비교','Compare an indoor alternative'],['delay','clock-4','90분 늦게 도착하면','Arriving 90 min late','비와 지연을 함께 반영','Add a delay to the rainy-day plan'],['return','plane-takeoff','복귀 시간이 부족하면','Running short on time','박물관에서 15:30 출발 가정','Leave the museum at 15:30']];
 return `<section class="demo-stage" aria-label="${t('공모전 데모 시나리오','Demo scenarios')}"><div class="demo-stage-heading"><div><span class="eyebrow">A LITTLE PREVIEW OF YOUR JOURNEY</span><h1>${t('계획이 바뀌는 순간,<br>직접 경험해 보세요.','Try a little change<br>of plans.')}</h1><p>${t('입력 없이 준비된 여행이에요. 같은 기본 여행에서 상황별 코스와 복귀 시간을 비교해 보세요.','Your sample trip is ready. Each situation starts from the same original plan for an easy comparison.')}</p></div><div class="demo-ticket"><span>ICN · T1</span><strong>07:00 <span>→</span> 20:00</strong><small>${t('송도 · 택시 · 예산 150,000원','Songdo · taxi · ₩150,000 budget')}</small><span class="demo-ticket-note">${t('입국 가능한 상황을 가정한 예시','Sample assumes entry eligibility')}</span></div></div><div class="demo-scenarios">${scenes.map(([id,ico,ko,en,sub,subEn],i)=>`<button class="demo-scenario ${state.demoScene===id?'active':''}" data-action="demo-scene" data-id="${id}" aria-pressed="${state.demoScene===id}"><span class="demo-scenario-top"><span>0${i+1}</span>${icon(ico)}</span><strong>${t(ko,en)}</strong><small>${t(sub,subEn)}</small></button>`).join('')}</div><p class="demo-disclosure">${icon('info')}${t('날씨·지연·시간·비용은 체험용 가정입니다. 관광정보·공항 경로 조회 버튼은 실제 데이터를 요청합니다.','Weather, delays, times and costs here are demo assumptions. Destination details and airport directions request real data.')}</p></section>`;
}
